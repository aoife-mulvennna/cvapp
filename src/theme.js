import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
  
      main: '#FEBCC7',
      light: '#FFFAFB',
      dark: '#FE67A6',
      contrastText: '#75005B',
  },

});

export default theme;
